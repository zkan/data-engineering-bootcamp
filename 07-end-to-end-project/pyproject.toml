[project]
name = "07-end-to-end-project"
version = "0.1.0"
description = ""
authors = [
    {name = "zkan",email = "zkan.cs@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
    "dbt-core (>=1.12.0,<2.0.0)",
    "dbt-bigquery (>=1.12.0,<2.0.0)"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
